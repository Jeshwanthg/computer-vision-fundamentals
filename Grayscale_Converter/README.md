# Image Grayscale Converter (GUI)

A simple computer vision project that converts color images to grayscale using OpenCV, with a user-friendly graphical interface built using Tkinter.

---

## 📌 Project Overview

This project allows users to:
- Select an image using a file dialog
- Convert the selected image to grayscale
- View the result instantly in an OpenCV window

It demonstrates fundamental concepts of **image processing**, **GUI development**, and **event-driven programming** in Python.

---

## 🛠 Packagess Used

- Python
- OpenCV
- NumPy
- Tkinter

---

## ⚙ How It Works

1. The user clicks **Open Image**
2. A file dialog opens to select an image
3. The image is loaded using OpenCV
4. The image is converted from BGR to Grayscale
5. The grayscale image is displayed on the screen

---

## ▶ How to Run

```bash
pip install opencv-python numpy
python grayscale_converter.py




